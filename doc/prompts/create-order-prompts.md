你好！现在的任务是模拟微信小程序请求，向第三方API发起查询车票数量和创建车票订单请求。
除了作为一名高级 Java 软件工程师，更是一名资深产品经理，我现在有如下功能：
1. 监控本周周一至周日的车次的车票数量，也可以指定具体的监控日期，有票则创建订单。
2. 监控下周周一至周五的车次的车票数量，也可以指定具体的监控日期，有票则创建订单。
3. 监控本周和下周车票数量需要提供本地 curl 工具可访问的 API。

具体的创建订单流程如下：
1. 监控车次的车票数量，有车票则继续，无车票则继续监控。
2. 查询用户可用的优惠券，选择第一张优惠券。
3. 验证订单是否能提交，如果验证成功则继续，失败则选择另一张优惠券重新验证。
4. 使用路线、车次、优惠券、上车车站、下车车站创建订单。
以上是我的需求，有不明白的地方，请你向我提问。
---------------------------------------------------------



---------------------------------------------------------

**现在，所有的需求都已清晰。**
请扮演“需求编译器”，执行以下操作：
1. 在项目根目录下创建一个名为 specs/001-monitor-and-buy-ticket/ 的目录。
2. 在该目录下创建 spec.md 文件。内容必须包含：
   * **用户故事**（通过CURL工具方便发出监控请求）
   * **功能性需求**（涵盖我们讨论的所有细节： 监控和下单流程、监控频率、只创建订单不支付）
   * **非功能性需求**（架构解耦、错误处理）
   * **验收标准**（列出具体的测试Case）
   * **输出格式示例**（创建订单成功）

请直接执行工具生成文件。
--------------------------------------------------------------

非常好。基于这份 `spec.md` 和我们的 `constitution.md`（特别是关于包内聚的原则），请为这个功能进行架构设计和技术选型。
1. 使用 SpringBoot 3.x 作为基础架构。
2. 外部 API 请求使用 RestClient 代码库。
3. 外部 API 请求封装为代码库，方便替换。
4. 业务逻辑代码放在`com.tqh.bus.ticket.service`。
5. 外部 API 代码放在`com.tqh.bus.ticket.integration`。


--------------------------------------------------------------------------

**Prompt 1: 生成技术方案**

`@./specs/001-monitor-and-buy-ticket/spec.md`

你现在是`tqh-bus-ticket`项目的首席架构师。你的任务是基于我提供的`spec.md`以及我们已有的`constitution.md`（你已自动加载），为项目生成一份详细的技术实现方案（`plan.md`）。

**技术栈约束 (必须遵循):**

- **语言**: Java (>=21)
- **Web框架**: 使用 SpringBoot 3.x 作为基础架构。
- **HTTP Client**: 外部 API 请求使用 RestClient 代码库。
- **接口数据序列化**: 所有接口的请求/响应报文使用 Spring 自带的 Jackson。

**方案内容要求 (必须包含):**

1.  **技术上下文总结:** 明确上述技术选型。
2.  **“合宪性”审查:** 逐条对照`constitution.md`的原则，检查并确认本技术方案符合所有条款（特别是包内聚、错误处理、TDD）。
3.  **项目结构细化:** 业务逻辑代码放在`com.tqh.bus.ticket.service`，外部 API 代码放在`com.tqh.bus.ticket.integration`。

请严格按照`@./.claude/templates/plan-template.md`的模板格式来组织你的输出（如果模板不存在，请自行设计一个结构清晰的Markdown格式）。

完成后，将生成的`plan.md`内容写入到`./specs/001-monitor-and-buy-ticket/plan.md`文件中。

----------------------------------------------------------------------------------

**Prompt 2: 生成任务列表**

方案非常完美。

现在，请扮演技术组长。请仔细阅读 `@./specs/001-monitor-and-buy-ticket/spec.md` 和 `@./specs/001-monitor-and-buy-ticket/plan.md`。

你的目标是将 `plan.md`中描述的技术方案，分解成一个**详尽的、原子化的、有依赖关系的、可被AI直接执行的任务列表**。

**关键要求：**
1.  **任务粒度：** 每个任务应该只涉及一个主要文件的修改或创建一个新文件。不要出现“实现所有功能”这种大任务。
2.  **TDD强制：** 根据`constitution.md`的“测试先行铁律”，**必须**先生成测试任务，后生成实现任务。
3.  **并行标记：** 对于没有依赖关系的任务，请标记 `[P]`。
4.  **阶段划分：** 按照`plan.md`的中步骤实现。
    *   **Phase 1: 基础设施**
    *   **Phase 2: 外部 API 层**
    *   **Phase 3: 业务逻辑层**
    *   **Phase 4: 控制器层**

完成后，将生成的任务列表写入到`./specs/001-monitor-and-buy-ticket/tasks.md`文件中。

--------------------------------------------------------------------------------------

`@./specs/001-monitor-and-buy-ticket/tasks.md`
`@./specs/001-monitor-and-buy-ticket/plan.md`

请按`tasks.md`计划执行 **Phase 1: 基础设施** 中的所有任务。

---------------------------------------------------------------
非常棒的Commit Message！

现在，请继续扮演项目维护者的角色，基于我们到目前为止的所有工作，为这次提交撰写一份清晰的Pull Request描述。

PR描述需要面向团队的其他审查者，包含以下几个部分：

1.  **背景（Context）：** 简要说明这个PR是为了解决什么问题，可以引用`spec.md`中的核心用户故事。
2.  **实现方案（Implementation）：** 概述一下我们是如何实现这个功能的（例如，监控车票和创建订单流程）。
3.  **测试（Testing）：** 说明我们采用了TDD，并且所有代码都有单元测试覆盖。
4.  **如何手动验证（How to Verify）：** 给出清晰的步骤，告诉审查者如何在本地运行和验证这个新功能。

-------------------------------------------------------------------
描述写得很好。现在，请直接调用 GitHub 工具，使用这段描述，为我的这次代码评审问题的修正创建一个 Pull Request。